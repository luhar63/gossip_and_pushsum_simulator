defmodule Project.Application do
  use Application

  def start(_type, [_nodesNumber, _topology, _algorithm]) do
    # Proj1.Supervisor.startFun()
    # Project.Main.start(nodesNumber, topology, algorithm)
  end
end
